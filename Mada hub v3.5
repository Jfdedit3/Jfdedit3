-- Mada Hub v3.5 - Interface Moderne avec Défilement
local TweenService = game:GetService("TweenService")
local NotificationLoad = loadstring(game:HttpGet('https://raw.githubusercontent.com/treeofplant/Notif/main/library.lua', true))()

-- Fonction pour ajouter des coins arrondis
local function ajouterCoinsArrondis(element, rayon)
    local coin = Instance.new("UICorner")
    coin.CornerRadius = UDim.new(0, rayon)
    coin.Parent = element
end

-- Fonction utilitaire pour créer une instance avec des propriétés spécifiées
local function creerElementUI(typeInstance, proprietes)
    local element = Instance.new(typeInstance)
    for propriete, valeur in pairs(proprietes) do
        element[propriete] = valeur
    end
    return element
end

local gui = Instance.new("ScreenGui")
gui.Name = "Mada hub v3.5"
gui.Parent = game.CoreGui

-- Cadre principal de taille réduite
local cadrePrincipal = creerElementUI("Frame", {
    Size = UDim2.new(0, 400, 0, 300),  -- Taille réduite
    Position = UDim2.new(0.5, -200, 0.5, -150),  -- Position ajustée
    BackgroundColor3 = Color3.fromRGB(25, 25, 35),
    BorderSizePixel = 0,
    Visible = false,
    Parent = gui
})
ajouterCoinsArrondis(cadrePrincipal, 20)

-- Dégradé de fond
local degrade = Instance.new("UIGradient")
degrade.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(30, 30, 45)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(20, 20, 30))
})
degrade.Rotation = 45
degrade.Parent = cadrePrincipal

-- Barre latérale réduite
local barreLaterale = creerElementUI("Frame", {
    Size = UDim2.new(0, 100, 1, 0),  -- Largeur réduite
    Position = UDim2.new(0, 0, 0, 0),
    BackgroundColor3 = Color3.fromRGB(20, 20, 30),
    BorderSizePixel = 0,
    Parent = cadrePrincipal
})
ajouterCoinsArrondis(barreLaterale, 20)

-- Logo ajusté
local cadreLogo = creerElementUI("Frame", {
    Size = UDim2.new(1, 0, 0, 60),
    Position = UDim2.new(0, 0, 0, 0),
    BackgroundTransparency = 1,
    Parent = barreLaterale
})

local titre = creerElementUI("TextLabel", {
    Size = UDim2.new(1, 0, 1, 0),
    BackgroundTransparency = 1,
    Font = Enum.Font.GothamBold,
    Text = "MADA\nHUB",
    TextColor3 = Color3.fromRGB(255, 255, 255),
    TextSize = 18,  -- Taille de texte réduite
    Parent = cadreLogo
})

-- Zone de défilement pour le contenu
local zoneDefilement = creerElementUI("ScrollingFrame", {
    Size = UDim2.new(0, 270, 1, -20),  -- Largeur ajustée
    Position = UDim2.new(0, 105, 0, 10),  -- Position ajustée
    BackgroundTransparency = 1,
    Parent = cadrePrincipal,
    CanvasSize = UDim2.new(0, 0, 0, 0),  -- Taille ajustée dynamiquement
    ScrollBarThickness = 8,
    ScrollBarImageColor3 = Color3.fromRGB(60, 60, 75)
})

-- Liste des boutons avec leurs configurations
local boutons = {
    {text = "KOLINSKI admin", url = "https://raw.githubusercontent.com/Jfdedit3/KOLINSKI/refs/heads/main/KOLINSKI.lua", icon = "⚡"},
    {text = "Cmd Fe", url = "https://raw.githubusercontent.com/lxte/cmd/main/testing-main.lua", icon = "🎮"},
    {text = "Honoka Executor", url = "https://raw.githubusercontent.com/Jfdedit3/honoka-executor/main/Honoka%20executor%20byz4trox", icon = "🔧"},
    {text = "Velocity Ware", url = "https://raw.githubusercontent.com/ImNotRox1/VWare/main/Universal.lua", icon = "🚀"},
    {text = "Nameless Music", url = "https://raw.githubusercontent.com/Jfdedit3/Nameless-hub-/refs/heads/main/Nameless%20musique", icon = "🎵"},
    {text = "Aimbot", url = "https://raw.githubusercontent.com/Jfdedit3/Aimbot-aim-assist-lock-/refs/heads/main/Aimbot-aim-assist-lock", icon = "🎯"},
    {text = "Fps Script", url = "https://raw.githubusercontent.com/Jfdedit3/Fps/refs/heads/main/fps.lua", icon = "📊"},
    {text = "MADA Admin", url = "https://raw.githubusercontent.com/Jfdedit3/Mada-admin-/refs/heads/main/MADA%20admin.lua", icon = "👑"}
}

-- Fonction pour créer des boutons stylisés
local function creerBoutonModerne(info, index)
    local bouton = creerElementUI("Frame", {
        Size = UDim2.new(0, 130, 0, 60),  -- Taille réduite
        Position = UDim2.new(((index-1)%2) * 0.5, 5, math.floor((index-1)/2) * 0.25, 5), -- Espacement ajusté
        BackgroundColor3 = Color3.fromRGB(35, 35, 50),
        Parent = zoneDefilement
    })
    ajouterCoinsArrondis(bouton, 12)

    local degradeBouton = Instance.new("UIGradient")
    degradeBouton.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(45, 45, 65)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(35, 35, 50))
    })
    degradeBouton.Rotation = 45
    degradeBouton.Parent = bouton

    -- Icône ajustée
    local icone = creerElementUI("TextLabel", {
        Size = UDim2.new(0, 25, 0, 25),  -- Taille de l'icône réduite
        Position = UDim2.new(0, 10, 0, 5),
        BackgroundTransparency = 1,
        Text = info.icon,
        TextSize = 20,  -- Taille de texte de l'icône réduite
        Font = Enum.Font.GothamBold,
        TextColor3 = Color3.fromRGB(255, 255, 255),
        Parent = bouton
    })

    -- Texte ajusté
    local texte = creerElementUI("TextLabel", {
        Size = UDim2.new(1, -20, 0, 20),
        Position = UDim2.new(0, 10, 0, 35),  -- Position ajustée
        BackgroundTransparency = 1,
        Text = info.text,
        TextSize = 10,  -- Taille de texte réduite
        Font = Enum.Font.GothamSemibold,
        TextColor3 = Color3.fromRGB(255, 255, 255),
        TextXAlignment = Enum.TextXAlignment.Left,
        Parent = bouton
    })

    local zoneClic = creerElementUI("TextButton", {
        Size = UDim2.new(1, 0, 1, 0),
        BackgroundTransparency = 1,
        Text = "",
        Parent = bouton
    })

    -- Animations
    zoneClic.MouseEnter:Connect(function()
        TweenService:Create(bouton, TweenInfo.new(0.3), {
            BackgroundColor3 = Color3.fromRGB(45, 45, 65)
        }):Play()
    end)

    zoneClic.MouseLeave:Connect(function()
        TweenService:Create(bouton, TweenInfo.new(0.3), {
            BackgroundColor3 = Color3.fromRGB(35, 35, 50)
        }):Play()
    end)

    zoneClic.MouseButton1Click:Connect(function()
        TweenService:Create(bouton, TweenInfo.new(0.1), {
            Size = UDim2.new(0, 125, 0, 55)
        }):Play()
        wait(0.1)
        TweenService:Create(bouton, TweenInfo.new(0.1), {
            Size = UDim2.new(0, 130, 0, 60)
        }):Play()

        NotificationLoad:NewNotification({
            Mode = "Custom",
            Title = info.text,
            Description = info.text .. " est en cours d'exécution.",
            Timeout = 3
        })

        local success, err = pcall(function()
            loadstring(game:HttpGet(info.url, true))()
        end)

        if not success then
            NotificationLoad:NewNotification({
                Mode = "Error",
                Title = "Erreur",
                Description = "Échec du chargement du script : " .. err,
                Timeout = 5
            })
        end
    end)
end

-- Créer les boutons
for i, infoBouton in ipairs(boutons) do
    creerBoutonModerne(infoBouton, i)
    -- Ajustement de la taille du Canvas pour que tous les boutons soient visibles
    zoneDefilement.CanvasSize = UDim2.new(0, 0, 0, math.ceil(#boutons / 2) * 65)
end

-- Bouton de fermeture ajusté
local boutonFermer = creerElementUI("TextButton", {
    Size = UDim2.new(0, 25, 0, 25),  -- Taille réduite
    Position = UDim2.new(1, -35, 0, 10),
    BackgroundColor3 = Color3.fromRGB(255, 70, 70),
    Text = "×",
    TextColor3 = Color3.fromRGB(255, 255, 255),
    TextSize = 20,  -- Taille de texte réduite
    Font = Enum.Font.GothamBold,
    Parent = cadrePrincipal
})
ajouterCoinsArrondis(boutonFermer, 15)

-- Animation de fermeture
boutonFermer.MouseButton1Click:Connect(function()
    local fondu = TweenService:Create(cadrePrincipal, TweenInfo.new(0.5), {
        BackgroundTransparency = 1
    })
    
    local contraction = TweenService:Create(cadrePrincipal, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 0, 0, 0),
        Position = UDim2.new(0.5, 0, 0.5, 0)
    })
    
    fondu:Play()
    contraction:Play()
    
    wait(0.5)
    gui:Destroy()
end)

-- Animation d'ouverture ajustée
local function afficherAnimationIntro()
    cadrePrincipal.Size = UDim2.new(0, 0, 0, 0)
    cadrePrincipal.Position = UDim2.new(0.5, 0, 0.5, 0)
    cadrePrincipal.BackgroundTransparency = 1
    cadrePrincipal.Visible = true
    
    local expansion = TweenService:Create(cadrePrincipal, TweenInfo.new(0.7, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 400, 0, 300),  -- Taille réduite
        Position = UDim2.new(0.5, -200, 0.5, -150)  -- Position ajustée
    })
    
    local apparition = TweenService:Create(cadrePrincipal, TweenInfo.new(0.5), {
        BackgroundTransparency = 0
    })
    
    expansion:Play()
    apparition:Play()
end

afficherAnimationIntro()
